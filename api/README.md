# API 规范

本目录用于存放 Go Image Server 的 API 规范与生成文件，例如：

- `swagger.json`
- `swagger.yaml`
- `docs.go`（由 `swag` 工具生成）

生成命令（在仓库根目录执行）：

```bash
go run ./scripts/swag-init.ps1  # 或在 PowerShell 中: ./scripts/swag-init.ps1
```

